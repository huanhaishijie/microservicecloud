spring:
  cloud:
    config:
      name: microservicecloud-config-client #需要从githup上读取的资源名称，注意没有yml后缀名
      profile: test  #本次访问的配制项
      label: master
      uri: http://config-5201.com:5201 #本微服务启动后先去找5201端口服务，通过springcloudConfig获取gitHup的服务地址

